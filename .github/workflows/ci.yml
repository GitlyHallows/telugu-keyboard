name: Telugu Keyboard CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  swift:
    name: Swift regression suite
    runs-on: macos-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run build, smoke, and quality tests
        run: ./script/ci.sh
